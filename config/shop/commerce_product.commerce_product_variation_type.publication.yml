uuid: c4d5e6f7-8901-2345-6789-0abcdef12345
langcode: en
status: true
dependencies:
  enforced:
    module:
      - commerce_product
id: publication
label: Publication
traits: {  }
# Note: the previous traits value (commerce_shipping_shippable) pointed at
# a non-existent commerce trait plugin and was a silent no-op. Shippability
# is now driven by the `weight` field on this bundle + the `shipments`
# field on commerce_order, both managed by saho_shop_shipping. The proper
# trait would be `purchasable_entity_shippable` but applying it via
# config sync would conflict with the explicit weight field, so we
# leave traits empty.
locked: false
orderItemType: default
generateTitle: true
