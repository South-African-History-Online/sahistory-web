{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["@playwright/mcp@latest", "--ignore-https-errors"],
      "env": {
        "PLAYWRIGHT_HEADLESS": "true"
      }
    }
  }
}
