{#
/**
 * @file
 * Custom pager template specifically for archive_pages view, page_2 display (Collections).
 *
 * Available variables:
 * - heading_id: Pagination heading ID
 * - items: List of pager items
 *   - first: Item for the first page
 *   - previous: Item for the previous page
 *   - next: Item for the next page
 *   - last: Item for the last page
 *   - pages: List of pages, keyed by page number
 * - current: The page number of the current page
 * - ellipses: If there are more pages than quantity
 */
#}

{# Use Radix pagination component with proper wrapper #}
{%
  include 'radix:pagination' with {
    items: items,
    current: current,
    ellipses: ellipses,
    alignment: 'center',
    pagination_utility_classes: ['saho-archives-pagination']
  }
%}
