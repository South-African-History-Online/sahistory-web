{#
/**
 * @file
 * Intentionally empty row template for the SAHO global search view.
 *
 * The "Open Record" search shell does not print {{ rows }}: results render
 * from record_rows / record_cards built in
 * saho_preprocess_views_view__saho_global_search() via the saho-index-table
 * and saho-archive-card SDCs (see views-view--saho-global-search--page-1).
 * This stub only exists so that any display printing {{ rows }} never falls
 * back to the default fields markup.
 */
#}
