{#
/**
 * @file
 * Template to display rows for the Archive Pages view in a modern card format.
 *
 * Available variables:
 * - title: The title of this group of rows.
 * - rows: A list of the view's row items.
 * - attributes: The row's HTML attributes.
 * - content_attributes: The row content's HTML attributes.
 * - row_attributes: HTML attributes for each row.
 * - view: The view object.
 * - default_row_class: A flag indicating whether default classes should be used on rows.
 *
 * @see template_preprocess_views_view_unformatted()
 */
#}

{# Add modern styling for each row as a card #}
{% if title %}
  <h3 class="h4 mb-4">{{ title }}</h3>
{% endif %}

{% for row in rows %}
  {# Use the row content directly instead of trying to extract from it #}
  {{ row.content }}
{% endfor %}