{#
/**
 * @file
 * A layout for the checkout registration form
 *
 * Available variables:
 * - form: The form.
 *
 * @ingroup themeable
 */
#}

{%
  set component_data = {
    'title': 'Create your account'|t,
    'description': 'Set a password to save your details for next time.'|t,
    'content': form,
    'icon': pane_icon|default(''),
    'variant': pane_variant|default('default'),
    'collapsible': pane_collapsible|default(false),
    'collapsed': pane_collapsed|default(false),
  }
%}

{{ include('saho_shop:content-pane', component_data) }}
