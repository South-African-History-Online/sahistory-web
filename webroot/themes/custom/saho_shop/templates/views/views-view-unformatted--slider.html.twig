{#
/**
 * @file
 * Bootstrap 5 Carousel items template for Slider view.
 *
 * Wraps each row in a carousel-item div for proper Bootstrap carousel functionality.
 *
 * Available variables:
 * - title: The title of this group of rows. May be empty.
 * - rows: A list of the view's row items.
 *   - attributes: The row's HTML attributes.
 *   - content: The row's content.
 * - view: The view object.
 * - default_row_class: A flag indicating whether default classes should be used on rows.
 */
#}
{% if title %}
  <h3>{{ title }}</h3>
{% endif %}

{% for row in rows %}
  <div class="carousel-item{% if loop.first %} active{% endif %}">
    {{- row.content -}}
  </div>
{% endfor %}
