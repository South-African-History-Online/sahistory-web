# DRAFT reference for issue #435 - not importable (lives in config/draft).
# Canonical version: config/sync/field.storage.node.field_caps_topic.yml
#
# THE SPINE FIELD. Shared by every resource bundle plus the legacy article and
# archive bundles; that shared reference is what makes a topic spine a single
# cross-bundle query. cardinality -1 so a resource can serve several topics;
# translatable so entity translations can localise per language.
langcode: en
status: true
dependencies:
  module:
    - node
    - taxonomy
id: node.field_caps_topic
field_name: field_caps_topic
entity_type: node
type: entity_reference
settings:
  target_type: taxonomy_term
module: core
locked: false
cardinality: -1
persist_with_no_fields: false
custom_storage: false
translatable: true
indexes: {  }
