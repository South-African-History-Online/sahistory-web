# DRAFT reference for issue #435 - not importable (lives in config/draft).
# Canonical: config/sync/field.field.node.presentation.field_classroom_resource_type.yml
#
# Single-value: a presentation is one resource type, which the topic spine
# reads to order this node inside its topic's lesson unit.
langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_classroom_resource_type
    - node.type.presentation
    - taxonomy.vocabulary.classroom_resource_type
id: node.presentation.field_classroom_resource_type
field_name: field_classroom_resource_type
entity_type: node
bundle: presentation
label: 'Resource type'
description: ''
required: false
translatable: true
default_value: {  }
default_value_callback: ''
settings:
  handler: 'default:filter_existing_terms'
  handler_settings:
    target_bundles:
      classroom_resource_type: classroom_resource_type
    auto_create: false
field_type: entity_reference
