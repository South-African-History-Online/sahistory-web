# DRAFT reference for issue #435 - not importable (lives in config/draft).
# Canonical: config/sync/field.storage.node.field_classroom_resource_type.yml
#
# Resource-type reference (classroom_resource_type vocabulary). cardinality 1:
# a node is exactly one resource type, which the topic spine reads to place and
# order the node within a topic's lesson unit. translatable for entity
# translations.
langcode: en
status: true
dependencies:
  module:
    - node
    - taxonomy
id: node.field_classroom_resource_type
field_name: field_classroom_resource_type
entity_type: node
type: entity_reference
settings:
  target_type: taxonomy_term
module: core
locked: false
cardinality: 1
persist_with_no_fields: false
custom_storage: false
translatable: true
indexes: {  }
