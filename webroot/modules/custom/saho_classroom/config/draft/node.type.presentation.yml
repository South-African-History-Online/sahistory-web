# DRAFT reference for issue #435 - not importable (lives in config/draft).
# Canonical version: config/sync/node.type.presentation.yml
#
# Worked example of a Classroom 2.0 resource bundle. The other four bundles
# (worksheet, activity, quiz, classroom_clip) share the same four classroom
# fields drafted alongside this file. Revisions on; unpublished-by-default is a
# per-bundle editorial choice (classroom_clip defaults to unpublished for
# review, presentation does not).
langcode: en
status: true
dependencies: {  }
name: Presentation
type: presentation
description: 'A CAPS-aligned classroom presentation (HTML-native deck or file).'
help: null
new_revision: true
preview_mode: 1
display_submitted: false
