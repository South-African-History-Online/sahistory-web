# DRAFT reference for issue #435 - not importable (lives in config/draft).
# Canonical: config/sync/field.field.node.presentation.field_caps_topic.yml
#
# The spine binding on the worked-example bundle. Every other resource bundle
# carries the same instance pointing at the caps_topic vocabulary; that shared
# binding is the topic spine.
langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_caps_topic
    - node.type.presentation
    - taxonomy.vocabulary.caps_topic
id: node.presentation.field_caps_topic
field_name: field_caps_topic
entity_type: node
bundle: presentation
label: 'CAPS topic'
description: ''
required: false
translatable: true
default_value: {  }
default_value_callback: ''
settings:
  handler: 'default:filter_existing_terms'
  handler_settings:
    target_bundles:
      caps_topic: caps_topic
    auto_create: false
field_type: entity_reference
