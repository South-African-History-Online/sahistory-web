# DRAFT reference for issue #435 - not importable (lives in config/draft).
# Canonical version: config/sync/taxonomy.vocabulary.classroom_resource_type.yml
#
# Resource-type facet. Terms (weighted, in teaching order): Topic overview,
# Presentation, Worksheet, Activity, Quiz, Clip, Glossary, Source-based
# questions, Essay questions, Exam paper. Two jobs: (1) type legacy
# article/archive material without changing its bundle, (2) supply the term
# weight the topic spine uses to order a topic's members.
langcode: en
status: true
dependencies: {  }
name: 'Classroom resource type'
vid: classroom_resource_type
description: 'Resource-type facet; orders the topic spine and types legacy content in place.'
weight: 0
new_revision: false
