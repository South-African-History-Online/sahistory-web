# DRAFT reference for issue #435 - not importable (lives in config/draft).
# Canonical version: config/sync/taxonomy.vocabulary.caps_topic.yml
#
# THE SPINE ANCHOR. One term per CAPS curriculum topic, children seeded under a
# per-grade parent from the official History curriculum. Every classroom
# resource references a term here through field_caps_topic, which is what lets
# a single topic assemble its full lesson unit (see ../../src/TopicSpine*.php).
langcode: en
status: true
dependencies: {  }
name: 'CAPS topic'
vid: caps_topic
description: 'CAPS curriculum topics - the anchor that links all resources for one topic.'
weight: 0
new_revision: false
