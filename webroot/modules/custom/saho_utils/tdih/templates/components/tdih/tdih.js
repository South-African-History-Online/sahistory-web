(function ($, Drupal) {
    Drupal.behaviors.tdihBlock = {
      attach: function (context, settings) {
        console.log('This Day in History block attached.');
      },
    };
  })(jQuery, Drupal);
  