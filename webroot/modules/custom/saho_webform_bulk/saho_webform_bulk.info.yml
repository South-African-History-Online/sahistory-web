name: 'SAHO Webform Bulk'
type: module
description: 'Lets submission admins apply Delete to every submission matching the current results filter, not only the current page.'
core_version_requirement: ^10 || ^11
package: SAHO
dependencies:
  - webform:webform
