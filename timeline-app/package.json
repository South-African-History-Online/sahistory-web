{
  "name": "saho-timeline-app",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "description": "SAHO timeline explorer - the Register and the Ruler",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "check": "svelte-check --tsconfig ./jsconfig.json || true"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.1.1",
    "svelte": "^5.38.1",
    "vite": "^7.1.2"
  }
}
